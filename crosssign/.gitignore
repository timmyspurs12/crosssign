# dependencies / build output
node_modules
.next
out
build

# env — keep the example, never the real keys
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# Rust / Stylus build output (never commit target/)
contract/target/

# deploy logs are noisy; sepolia.json is the record and IS committed
contract/deployments/*.log

# QA screenshots
qa/shots/

# OS
.DS_Store
